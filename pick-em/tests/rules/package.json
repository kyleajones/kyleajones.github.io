{
  "name": "pickem-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore Rules emulator tests for pick-em/firestore.rules",
  "scripts": {
    "test": "firebase emulators:exec --project=demo-pickem \"node --test *.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.0.0",
    "firebase-tools": "^13.0.0"
  }
}
